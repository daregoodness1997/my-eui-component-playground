import React from 'react';
import { EuiButton } from '@elastic/eui';

const Button = () => {
  return (
    <EuiButton color='primary' size='m' fill>
      Label
    </EuiButton>
  );
};

export default Button;
